import {
  Navbar, 
  NavbarProps, 
  NavbarBrand, 
  NavbarContent, 
  NavbarItem, 
  NavbarMenuToggle,
  NavbarMenu,
  NavbarMenuItem
} from "@heroui/react";

interface TimelineNavProps extends NavbarProps {
  editing? : boolean
}

const TimelineNav = (props:TimelineNavProps) => {
  <Navbar></Navbar>
}

export default TimelineNav;